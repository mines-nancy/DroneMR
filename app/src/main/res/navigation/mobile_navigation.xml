<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.dronemr.ui.home.HomeFragment"
        android:label="@string/menu_map"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_gallery"
        android:name="com.example.dronemr.ui.mainCamera.MainCameraFragment"
        android:label="@string/menu_main_camera"
        tools:layout="@layout/fragment_main_camera" />

    <fragment
        android:id="@+id/nav_camera"
        android:name="com.example.dronemr.ui.verticalCamera.VerticalCameraFragment"
        android:label="@string/menu_vertical_camera"
        tools:layout="@layout/fragment_vertical_camera" />
</navigation>